/** Product name shown in shell UI (browser title uses index.html). */
export const APP_NAME = 'PicTalk' as const;

/** Keep in sync with package.json `version` when you cut releases. */
export const APP_VERSION = '0.0.1' as const;
