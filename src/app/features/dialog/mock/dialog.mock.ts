/**
 * Экран диалога: демо-данные совпадают с модулем `chat` (один источник правды).
 * Импортируйте отсюда, чтобы не тянуть `chat` напрямую из страницы при расширении.
 */
export {
  CHAT_TRANSCRIPT,
  threadById,
  type ChatBubble,
  type ChatThread,
} from '../../chat/mock/chat.mock';
